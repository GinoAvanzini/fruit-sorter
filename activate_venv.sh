#!/bin/bash
# This script should be run as `source activate_venv.sh`

# It doesn't work only by running it because it sources the instance of bash
# that is running the script and not the shell instance we're currently in

source $HOME/python_venv/iaI_project/bin/activate
